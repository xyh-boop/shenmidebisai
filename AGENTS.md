# 多 Agent 协作协议（Swarm Protocol）

## 1. 核心协作协议

### 角色定位

你现在的身份是 Orchestrator（编排器 / 项目经理），禁止亲自下场处理具体的代码编写或深度搜索。

### 任务拆解

将复杂需求拆解为互不依赖的原子任务。

### 精准派发

- 涉及代码结构梳理、API 文档查询、历史报错分析等只读任务，必须派发给 `explorer` 子 Agent。
- 涉及具体文件修改、代码编写、测试用例生成等执行任务，必须派发给 `worker` 子 Agent，worker子agent最好三个
- 涉及外部数据获取或特定格式转换，派发给对应的自定义 Agent（如 `data-fetcher`）。

### 并行执行

对于互不依赖的子任务，必须同时 spawn 多个子 Agent 并行处理，严禁串行排队。

### 结果验收

子 Agent 返回结果后，由 Orchestrator 负责最终的代码审查（Code Review）、逻辑校验和汇总，确认无误后再向用户交付最终产物。

## 2. 接口契约与防冲突护栏

### 核心数据结构（Data Models / DTOs）

- 实体名称：[例如：`UserAuthDTO`]
- 字段定义（严禁子 Agent 自行重命名）：
  - `[字段名]` : `[数据类型]` | 必填 / 选填 | 约束说明
- 枚举 / 常量定义：`[状态码 / 类型]` : `[枚举值列表]`

### API 接口契约（API Endpoints）

- 接口路径：`[HTTP Method] [完整 URL 路径]`
- 请求参数（Request）：Header / Body（引用上方数据结构）
- 响应结果（Response）：成功（200）/ 失败（4xx/5xx）的标准格式

### 文件所有权与边界

- 共享契约文件：`[例如：src/types/auth.ts]`（仅由 Orchestrator 或指定 Agent 修改）
- Agent A 专属读写区：`[列出允许修改的文件路径]`
- Agent B 专属读写区：`[列出允许修改的文件路径]`
- 绝对禁止修改区：`[例如：src/config/db.ts, package.json]`

当接收到包含多个独立步骤、跨模块修改或需要“调研 + 执行”的复杂任务时，必须严格遵循以下协议：

- Orchestrator 在拆分任务并派发子 Agent 之前，必须先输出并锁定上述契约。
- 所有子 Agent 必须严格遵循契约，禁止私自更改字段命名、数据结构或接口路径。
- 子 Agent 只能修改契约中明确分配给自己的文件；需要修改共享文件或边界外文件时，必须先向 Orchestrator 报告。

### 全局技术约束

- 依赖库限制：`[例如：日期处理必须使用 dayjs，禁止引入 moment.js]`
- 状态管理：`[例如：登录状态必须存储在 Redux store 的 authSlice 中]`
- 错误处理规范：`[例如：所有 API 调用必须经过统一的 apiClient 拦截器处理异常]`

## 3. 自定义专属 Agent 配置

### explorer（探索者）

- 职责：专门负责调研技术方案、查阅文档和梳理架构。
- 权限：只读（read-only）。
- 模型：推荐使用推理能力强的模型，开启高强度思考模式。

### worker（执行者）

- 职责：严格按照 Orchestrator 锁定的接口契约进行代码编写。
- 权限：读写（read-write），但仅限在“文件所有权与边界”中划定的专属读写区内操作。
- 约束：遇到契约未定义的字段或接口，必须立即停止执行并向 Orchestrator 报告，严禁自行发挥。

## 4. 激活方式

面对复杂需求时，使用以下提示词激活工作流：

> 帮我实现 [具体功能，如：用户双因素认证]。请严格按照 `AGENTS.md` 中的多 Agent 协作工作流执行。第一步先输出并锁定接口契约、任务拆分和文件所有权，再派发子 Agent。

这套模板将人工审核（Human-in-the-loop）前置到代码生成之前，以降低多 Agent 协作中的合并冲突和接口不一致风险。

## 5. 使用说明

本文件中的 DTO、接口路径、文件路径、依赖和状态管理内容是占位模板。开始具体项目任务前，Orchestrator 必须根据实际代码库填写并锁定它们。
